# Services

Future independently deployable services live here. Stage 1 keeps the operational core inside the FastAPI backend while preserving service boundaries for optimization workers, simulation engines, AI copilots, and event processors.
